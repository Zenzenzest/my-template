export default function LandingPage() {
  return (
    <h1>
      Install @release-it/conventional-changelog (its incompatible with
      release-it v20 at the time this was made, maybe it is compatible now)
    </h1>
  );
}
